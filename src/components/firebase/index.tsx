import FirebaseContext, { withFirebase, ContextProp } from './context';
import Firebase from './firebase';
 
export default Firebase;
 
export { FirebaseContext, withFirebase };
export declare type FirebaseContextProp = ContextProp;